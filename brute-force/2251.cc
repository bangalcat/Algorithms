#include <bits/stdc++.h>

using namespace std;

int main() {
    int A, B, C;
    int currA, currB, currC;
    cin >> A >> B >> C;
    currA = 0, currB = 0, currC = C;
    bool check[201][201] = {0};
    set<int> ret;
    ret.insert(C);
    return 0;
}