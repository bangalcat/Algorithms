
#include <bits/stdc++.h>

using namespace std;

int main(){
    int T; cin >> T;

    for(int tc = 1; tc <= T; ++tc) {
        int n, k;
        cin >> n >> k;

        cout << "Case #" << tc << ": ";
    }
    return 0;
}