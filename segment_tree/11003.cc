#include <bits/stdc++.h>

using namespace std;

struct RMQ{
    vector<int> tree;
    int length;
    void insert(int nd, int ind, int ndl, int ndr, int a){
        if(ind < ndl || ind > ndr) return;
        if(ndl == ndr) {
        }
    }
};

int main(){
    
    return 0;
}